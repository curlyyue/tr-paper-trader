# secrets — never commit
.env

# python
.venv/
__pycache__/
*.pyc

# generated dashboard (published to Pages / uploaded as artifact instead)
dashboard/

# logs
data/*.log

# NOTE: data/*.db is intentionally NOT ignored — the SQLite ledgers are the
# agent's persistent state and are committed back to the repo after each run.
